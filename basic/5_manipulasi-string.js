const text = "Panjang text length?";
console.log(text.length + ' karekater');

const fullName = 'Prayoga Eka Ardiansyah';
const age = 21;
const hobby = 'coding';

const literal = `hallo, nama saya ${fullName}, saya berumur ${age}, hobby saya ${hobby}.`;
console.log(literal);

const obj = {
    saya: 'prayogaea',
    istri: 'putriajeng'
}
const object = `hallo, nama saya ${obj.saya}, dan istri saya ${obj.istri}.`;
console.log(object);